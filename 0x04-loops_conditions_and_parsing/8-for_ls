#!/usr/bin/env bash
# Lists visible files in current directory, showing only parts after first dash

for file in *; do
    if [ -f "$file" ] && [[ "$file" == *-* ]] && [[ "$file" != .* ]]; then
        echo "${file#*-}"
    fi
done
