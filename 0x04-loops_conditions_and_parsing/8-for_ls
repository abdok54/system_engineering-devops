#!/usr/bin/env bash
# This script lists files in current directory, showing only parts after first dash

for file in *; do
    if [ -f "$file" ] && [[ "$file" != .* ]]; then
        echo "${file#*-}"
    fi
done
